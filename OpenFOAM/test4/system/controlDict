
/*--------------------------------*- C++ -*----------------------------------*/

// File was generated by SwiftBlock, a Blender 3D addon.

FoamFile
{
    version     2.0;
    format      ascii;
    class       dictionary;
    object      controlDict;
}
// * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * //

application     simpleFoam;   // steady incomprimibile
startFrom       startTime;
startTime       0;
stopAt          endTime;
endTime         2000;         // o finché converge
deltaT          1;            // fittizio per steady; è nIter outer PIMPLE
writeControl    timeStep;
writeInterval   200;
purgeWrite      0;
writeFormat     ascii;
writePrecision  7;
writeCompression off;
timeFormat      general;
timePrecision   6;
runTimeModifiable yes;

functions
{
    cushionP_avg
    {
        type            patchAverage;
        libs            ("libfieldFunctionObjects.so");
        enabled         true;
        regionType      patch;
        name            cushionTopWall;  // metti il nome reale del patch
        fields          (p);
        writeControl    timeStep;
        writeInterval   10;
    }
}



// ************************************************************************* //

